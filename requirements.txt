# Guardian — 统一依赖文件
# 安装: pip install -r requirements.txt
# 国内镜像: pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# ─────────────────────────────────────────────
# 核心深度学习框架
# ─────────────────────────────────────────────
torch>=1.4.0
torchvision>=0.5.0

# ─────────────────────────────────────────────
# 人流量统计（YOLO + Keras）
# ─────────────────────────────────────────────
tensorflow>=1.14,<2.0
keras>=2.2.4,<2.4
h5py<3.0.0   # Keras .h5 权重兼容性

# ─────────────────────────────────────────────
# 计算机视觉 & 图像处理
# ─────────────────────────────────────────────
opencv-python>=3.4.0
numpy>=1.17.0
Pillow>=7.0.0
matplotlib>=3.0.0
scipy>=1.3.0

# ─────────────────────────────────────────────
# COCO 评估（OpenPose 训练验证）
# ─────────────────────────────────────────────
pycocotools>=2.0.0

# ─────────────────────────────────────────────
# 训练工具
# ─────────────────────────────────────────────
tensorboard>=2.0.0
tqdm>=4.40.0
PyYAML>=5.3.0

# ─────────────────────────────────────────────
# 可选：混合精度训练（需手动安装 NVIDIA Apex）
# ─────────────────────────────────────────────
# git clone https://github.com/NVIDIA/apex && cd apex
# pip install -v --no-cache-dir --global-option="--cpp_ext" --global-option="--cuda_ext" .
